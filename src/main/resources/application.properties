spring.profiles.active=local
spring.config.import=optional:file:.env[.properties]

spring.application.name=le phare culturel
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=${TIME_ZONE}
spring.jpa.hibernate.ddl-auto=update
server.servlet.context-path=/api/v1
spring.threads.virtual.enabled=true


spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=${MAIL_AUTH}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_TTLS}


app.url=${APP_URL}
spring.cache.type=simple

jwt.accessTokenExpirationMinutes=${JWT_ACCESS_TOKEN_EXPIRATION_MINUTES:10}
jwt.refreshTokenExpirationMinutes=${JWT_REFRESH_TOKEN_EXPIRATION_MINUTES:1440}

openagenda.api.agenda.id=57621068
openagenda.api.key=dd5c193e8af34e65b7654f7ba60b3e4f
